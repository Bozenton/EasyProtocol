classdef EP_ToMakePacket
    properties
        id;
        inst_idx;
        p_packet_buf;
        packet_buf_capacity;
        data_length;
        generated_packet_length;
        is_init;
    end

end

